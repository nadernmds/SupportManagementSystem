@model IEnumerable<SupportManagemantSystem.Models.Task>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_PupularLayout.cshtml";
}

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.decription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.spentTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Company.name)
        </th>
        <th>
            @Html.DisplayName("پروژه")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ticket.title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.decription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.spentTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Company.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ticket.title)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.taskID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.taskID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.taskID })
        </td>
    </tr>
}

</table>
