@model IEnumerable<SupportManagemantSystem.Models.UserGroup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_PupularLayout.cshtml";
}

<h2>گروه کاربری</h2>

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.userGroupID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.userGroupID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.userGroupID })
        </td>
    </tr>
}

</table>
